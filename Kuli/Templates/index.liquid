<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ Items.Title }}</title>
</head>
<body>
    {% for postFragment in Fragments %}
        {{ postFragment.Html }}
    {% endfor %}
</body>
</html>